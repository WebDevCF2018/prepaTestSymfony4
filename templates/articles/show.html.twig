{% extends 'template.html.twig' %}

{% block title %}Articles{% endblock %}
{% block menu %}
    <ul class="navbar-nav ml-auto">
        <li class="nav-item active">
            <a class="nav-link" href="{{ path("articles_index") }}">Accueil de l'administration
                <span class="sr-only">(current)</span>
            </a>
        </li>
        <li class="nav-item"> <a class="nav-link" href="{{ path('the_logout')}}">Déconnexion</a></li>
    </ul>
{% endblock %}
{% block contenu %}
    <h1>Articles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idarticles</th>
                <td>{{ article.idarticles }}</td>
            </tr>
            <tr>
                <th>Thetitle</th>
                <td>{{ article.thetitle }}</td>
            </tr>
            <tr>
                <th>Thetext</th>
                <td>{{ article.thetext }}</td>
            </tr>
            <tr>
                <th>Thedate</th>
                <td>{{ article.thedate ? article.thedate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('articles_index') }}">back to list</a>

    <a href="{{ path('articles_edit', {'idarticles': article.idarticles}) }}">edit</a>

    {{ include('articles/_delete_form.html.twig') }}
{% endblock %}
